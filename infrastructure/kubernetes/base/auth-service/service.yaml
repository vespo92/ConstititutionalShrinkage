# Constitutional Shrinkage - Auth Service
# Agent_14: DevOps & CI/CD Enhancement

apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: constitutional
  labels:
    app: auth-service
spec:
  type: ClusterIP
  selector:
    app: auth-service
  ports:
    - name: http
      port: 80
      targetPort: 3002
      protocol: TCP
